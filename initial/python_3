def making_change(price, denominations):
    """
        return the minimum number of coins that add up to the price
    """
    return None

if __name__ == '__main__':
    print("Example:")
    print(making_change(8, [1, 3, 5]))
    
    #These "asserts" using only for self-checking and not necessary for auto-testing
    assert making_change(8, [1, 3, 5]) == 2
    assert making_change(12, [1, 4, 5]) == 3
    print('Done')
